#include <cmath>
#include <iostream>

using namespace std;   
int main(char* args[])
{
	int a;
	cin >> a;
	cout << endl << "Sgr " << pow(a, 2);
	cout << endl << "Spp " << 6 * pow(a, 2);
	cout << endl << "Sgr " << a * a * a;
	return 0;
}
